// ===== Shared print CSS functions =====
// Re-exports from printUtils.ts (the original location).

export {
  getFormTemplateCSS,
  getSharedPrintCSS,
  getLetterheadTableCSS,
} from '../printUtils';
